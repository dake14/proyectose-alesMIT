# Proyecto de Clasificación de Latidos Cardíacos usando MIT-BIH (ECG)

Este proyecto implementa un pipeline completo para **clasificar latidos cardíacos (ECG)** como:

- **N → Normal**
- **V → Ventricular**

Usando la base de datos **MIT-BIH Arrhythmia**, filtrado digital, extracción de latidos, normalización, remuestreo, PCA y un clasificador **KNN**.

---

# 🧠 Pipeline General del Proyecto


    ┌──────────────────────────────────────┐
    │      1. Descarga MIT-BIH (wfdb)      │
    └───────────────┬──────────────────────┘
                    ▼
          ┌───────────────────┐
          │ 2. Carga de ECG   │
          └───────────────────┘
                    ▼
    ┌──────────────────────────────────────┐
    │   3. Filtrado: High-pass + Bandpass  │
    └──────────────────────────────────────┘
                    ▼
          ┌───────────────────┐
          │ 4. Segmentación    │ ← ventanas centradas en picos R
          └───────────────────┘
                    ▼
    ┌──────────────────────────────────────┐
    │     5. Normalización (z-score)       │
    └──────────────────────────────────────┘
                    ▼
          ┌───────────────────┐
          │ 6. Remuestreo     │ ← latidos → 100 muestras
          └───────────────────┘
                    ▼
    ┌──────────────────────────────────────┐
    │ 7. División Train/Test por paciente  │
    └──────────────────────────────────────┘
                    ▼
          ┌───────────────────┐
          │    8. PCA (15)    │
          └───────────────────┘
                    ▼
    ┌──────────────────────────────────────┐
    │      9. KNN (k = 3) + Métricas       │
    └──────────────────────────────────────┘



---

# 📦 Librerías utilizadas

Este proyecto funciona con estas librerías:

- wfdb  
- numpy  
- scipy  
- scikit-learn  
- matplotlib  

Un archivo `requirements.txt` está incluido.

---

# 🐍 Crear el entorno virtual `.venv`

### 1. Crear el entorno

```bash
python -m venv .venv
.venv\Scripts\Activate.ps1

### Descargar todas las librerias
pip install wfdb numpy scipy scikit-learn matplotlib
#  python download_data.py  es el archivo de descargar las datos de mit_data
# el codigo lectura es el codigo del proyecto.
